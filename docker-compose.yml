services:
  insta-manager:
    image: python:3.11-slim
    working_dir: /app
    command: bash -lc "pip install -r requirements.txt && python app.py"
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - ./:/app
      - ./media:/app/media
      - ./logs:/app/logs
